const getAllLeagues = async () => {
    // Logic here to retrieve all leagues from db
    return
}

const addLeague = async (/* some parameter to add a league to DB, probably just its name */) => {
    // Logic here to add a league to the DB
    return
}

export { getAllLeagues, addLeague };